# feetech-servo v0.7.1 + condition-tolerant servo reads

**Date:** 2026-09-04
**Branch:** `feetech-0.7-condition-reads` (from `main` @ 7a33fae)
**Status:** approved design, pre-implementation

## Problem

`internal/controller` discards a valid servo reading whenever the servo sets any status
flag. An overloaded servo (a gripper clamped on a part, a hot joint) is therefore
unreadable: `ServoPositionPercent` and `AnyServoMoving` return an error and no data. The
arm's `dwellUntilNear`, `WaitForServosToStop`, `IsMoving`, and the gripper's position
reads all inherit that blindness.

`feetech-servo` v0.7.0 fixed the library half: reads return the decoded value *and* an
error carrying the condition flags, and `feetech.ConditionStatus(err) (StatusError, bool)`
reports whether the payload alongside an error is usable (true for condition flags,
false for transport failures and request-rejection flags). Open PR #42 adopts this in
`internal/controller` but is stacked on two gripper PRs (#40, #39) that will take longer
to land. This workstream lands only the dependency bump and the read-path adoption on
`main`, so #42 shrinks to the gripper latch.

## Scope

**In:**

1. `go.mod`/`go.sum`: `github.com/hipsterbrown/feetech-servo v0.6.1` → **v0.7.1**.
2. `internal/controller/manager.go`
   - `ServoPositionPercent(ctx, id) (percent float64, raw int, condition feetech.StatusError, err error)`
   - `AnyServoMoving(ctx, ids) (moving bool, condition feetech.StatusError, err error)`
   - Both: `if flags, ok := feetech.ConditionStatus(err); ok { condition = flags; err = nil }`
     before the existing error return, so the reading is kept and the flags travel with it.
   - `WaitForServosToStop` adapts to the 3-value `AnyServoMoving` and drops the flags
     (a condition does not change whether the servo is moving).
3. `components/arm/motion.go`: the two `AnyServoMoving` callers (`IsMoving` path and the
   dwell/stall path) adopt the 3-value return and drop the flags.
4. `internal/servocmd/servo_commands.go`
   - `ServoOps.ServoPositionPercent` grows the `condition` return (structural interface;
     `*controller.ControllerHandle` satisfies it implicitly).
   - `CmdServoPosition` adds `"condition": <flags>` to its response map when non-zero,
     via `ConditionKey = "condition"`, `withCondition(resp, flags)`, and
     `ConditionArg(resp) feetech.StatusError` for the consuming side. A field, not an
     error, because a typed error does not survive the DoCommand boundary to a remote arm.
5. `internal/testfake`
   - `FakeServoOps`: `Condition feetech.StatusError` field, 4-value `ServoPositionPercent`.
   - `FakeTransport`: `status map[int]feetech.StatusError`, `SetStatus(id, flags)`, and
     the response packet's status byte set from it, so condition flags can be injected on
     the real protocol path. Adapted around `main`'s existing `regWrites` field.
6. Tests: `TestServoReadsSurviveAConditionFlag` (position + moving halves; no
   `ServoLoad` half), servocmd test that `servo_position` carries `condition`.
7. `CLAUDE.md`: one fresh gotcha (below). `docs/arm.md`/`docs/gripper.md`: note the
   `condition` key on `servo_position`.

**Out (stay in #42):** `ServoLoad`/`servo_load`/`get_load`; `wait_motion.go` motion-start
wait; the gripper grasp latch; `is_moving_test.go` logger change; #42's CLAUDE.md hunks
(they patch text that does not exist on `main`).

## Why v0.7.1 and not v0.7.0

v0.7.1 adds no API; it changes **writes**: `Bus.WriteRegister`, `RegWrite`, and the
sync-write path return `nil` on condition-only flags (previously an error), and
`Servo.SetID` updates its cached id when only the EEPROM re-lock failed. Every write site
on `main` (`calibrated_servo.go`, `manager.go`, calibration torque-enable) treats a write
error as fatal, and none infers "overloaded" from one, so this is strictly fewer spurious
failures. `Servo.Ping` tolerating flags (v0.7.0) also stops a clamped gripper from failing
arm init and discovery for free. #42 was written against v0.7.0; #42 will pick up v0.7.1
when it rebases.

## Method

`git cherry-pick` is not viable: #42's commits touch `wait_motion.go` (absent on main),
`gripper.go` (diverged), and `ServoLoad` (absent). One hand-written commit, using
`git show 460bba9` and `4012f36` on `origin/gripper-grasp-latch` as the reference for
each hunk. TDD per hunk: inject a status flag through `FakeTransport.SetStatus`, assert
the read returns data + flags, then make it pass.

## Testing

- `go test ./...`, `go test -race ./internal/... ./components/arm/...`, `go vet ./...`,
  `gofmt -s -l .` clean.
- Mutation check: revert the `ConditionStatus` branch in `AnyServoMoving`; both halves of
  `TestServoReadsSurviveAConditionFlag` must fail.
- No hardware needed. Hardware smoke worth doing after merge: clamp the gripper by hand
  and confirm `servo_position` on the arm returns a reading with `condition` set.

## CLAUDE.md gotcha to add

> **A servo condition flag is data, not a failed read.** `ServoPositionPercent` and
> `AnyServoMoving` keep the reading when `feetech.ConditionStatus(err)` says the payload
> is usable (overload/overheat/voltage), and return the flags alongside it; only transport
> failures and request-rejection flags still error. `servocmd` carries the flags in a
> `condition` response field rather than an error because a typed error does not survive
> the DoCommand boundary to a remote arm. Since feetech-servo v0.7.1 *writes* also return
> nil on condition-only flags, so nothing on main can infer "overloaded" from a write
> failure.

## Risks

- `condition` has no production consumer on `main` until #42 lands; it is the wire
  contract #42 rebases onto. Acceptable, small.
- Three `AnyServoMoving` callers and the structural `ServoOps` interface make a missed
  call site a compile error, not a silent one.
